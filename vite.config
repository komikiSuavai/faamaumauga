// vite.config.ts
// Vite configuration with the React plugin.
// Edit the server.port if you want a different local port.

import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  // dev server options
  server: {
    port: 5173 // change if this port conflicts on your machine
  }
});